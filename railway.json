{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "echo 'App desativada' && exit 0",
    "restartPolicyType": "NEVER"
  }
}
